# Generated by Django 5.2.15 on 2026-09-12 15:40

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0080_seed_updates_nav_item'),
    ]

    operations = [
        migrations.AddField(
            model_name='remotedjconfig',
            name='reconnect_grace_seconds',
            field=models.PositiveIntegerField(default=10, help_text="P1 1.5 Pass B1 -- how long an established Remote DJ session may stay in a muted 'Reconnecting' state after a recoverable transport loss (WebRTC PeerConnectionState DISCONNECTED) before the engine finalizes it and releases the slot, instead of waiting for GStreamer/libnice's own much longer native ICE failure timeout (observed ~49s in production field evidence). Read fresh every time a transient disconnect starts a new recovery-grace timer -- applies immediately, no engine restart or new session required. Bounded to 3-30 seconds."),
        ),
    ]
