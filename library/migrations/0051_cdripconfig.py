# Generated by Django 5.2.15 on 2026-07-14 21:30

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0050_cdripjob'),
    ]

    operations = [
        migrations.CreateModel(
            name='CDRipConfig',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('device', models.CharField(default='/dev/sr0', help_text='Path to the CD drive device node (usually /dev/sr0).', max_length=64)),
                ('drive_read_offset', models.IntegerField(default=6, help_text="AccurateRip drive read offset in samples for THIS drive model. Look up your drive at accuraterip.com/driveoffsets.htm. Wrong offset means the audio bytes are shifted vs community reference rips, so every track gets 'AR nomatch' -- the audio still plays fine, but the byte-identical verification won't pass. Default (+6) is correct for the hp PLDS DVDRW DU8AESH shipped with this box.")),
                ('require_accurate_rip', models.BooleanField(default=False, help_text="If checked, tracks that don't verify against AccurateRip are rejected outright. If unchecked (the default), non-verifying tracks are still imported with a warning badge -- appropriate since many CD-Rs and older pressings aren't in the AccurateRip database and their rips are still perfectly usable.")),
                ('staging_root', models.CharField(default='/srv/isadoraair/rip_staging', help_text='Scratch directory for in-progress rips before the post-processor moves them into the library. Must be writable by the gunicorn service user.', max_length=512)),
            ],
            options={
                'verbose_name': 'CD Rip Configuration',
                'verbose_name_plural': 'CD Rip Configuration',
            },
        ),
    ]
