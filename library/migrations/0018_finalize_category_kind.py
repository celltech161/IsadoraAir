# Generated by Django 5.2.15 on 2026-07-04 18:43

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0017_seed_category_kinds'),
    ]

    operations = [
        # Drop the old CharField+choices — kind_fk (populated by the
        # previous data migration) is the real value going forward.
        migrations.RemoveField(
            model_name='category',
            name='kind',
        ),
        # Rename kind_fk -> kind, preserving the FK data already
        # populated (a RenameField keeps the column/data intact, unlike
        # letting makemigrations autodetect this as RemoveField+AlterField
        # on the old CharField, which would try to cast a varchar
        # straight into an integer FK id).
        migrations.RenameField(
            model_name='category',
            old_name='kind_fk',
            new_name='kind',
        ),
        # Now safe to require it (every row was populated in 0017).
        migrations.AlterField(
            model_name='category',
            name='kind',
            field=models.ForeignKey(
                on_delete=django.db.models.deletion.PROTECT,
                related_name='categories',
                to='library.categorykind',
            ),
        ),
    ]
