# Generated by Django 5.2.15 on 2026-07-24 04:17

import django.db.models.deletion
from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0066_fxcart_duration_seconds'),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name='VoiceTrackConfig',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('program_duck_db', models.FloatField(default=-6.0, help_text='dB to duck the deck bus (music) while a VT is playing. Negative attenuates; 0 = no duck. Applied to a dedicated vt_duck_gain element in the pipeline, distinct from the mic/remote-DJ duck_gain so VT ducking and mic ducking can be tuned independently.')),
                ('duck_ramp_ms', models.PositiveIntegerField(default=300, help_text='Ramp duration for the duck attack (VT fires) and release (VT ends). 300ms is a comfortable radio-transition ramp.')),
                ('min_gap_ms', models.PositiveIntegerField(default=200, help_text="Silence between outro-VT ending and intro-VT starting, so the two VTs don't audibly bump into each other.")),
            ],
            options={
                'verbose_name': 'Voice Track Config',
                'verbose_name_plural': 'Voice Track Config',
            },
        ),
        migrations.CreateModel(
            name='VoiceTrack',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('position', models.CharField(choices=[('outro', "Outro (plays after outgoing track's outro_starts)"), ('intro', "Intro (plays before incoming track's intro_until)")], max_length=5)),
                ('filepath', models.CharField(help_text='Absolute path to the audio file (typically under /srv/isadoraair/voicetracks/).', max_length=512)),
                ('duration_seconds', models.FloatField(blank=True, editable=False, help_text="Auto-populated from mutagen at save. Drives the engine's underlap-delay math for the intro-VT fit calculation.", null=True)),
                ('gain_db', models.FloatField(default=0.0, help_text="Per-VT gain trim, dB. Applied at fire time on top of the FX bus volume. Range roughly -12 to +6 -- for a specific VT that's noticeably quieter or louder than typical; adjust program_duck_db in VoiceTrackConfig if the music-under-VT balance is systemically off across all VTs.")),
                ('recorded_at', models.DateTimeField(auto_now_add=True)),
                ('edited_at', models.DateTimeField(blank=True, null=True)),
                ('source', models.CharField(choices=[('browser', 'Browser recording (MediaRecorder + upload)'), ('studio', 'Studio input recording (Phase 2)'), ('import', 'Imported / manually placed file')], default='browser', max_length=10)),
                ('recorded_by', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='voicetracks_recorded', to=settings.AUTH_USER_MODEL)),
                ('track', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='voicetracks', to='library.track')),
            ],
            options={
                'verbose_name': 'Voice Track',
                'verbose_name_plural': 'Voice Tracks',
                'ordering': ['track', 'position'],
                'unique_together': {('track', 'position')},
            },
        ),
    ]
