# Generated by Django 5.2.15 on 2026-07-07 03:58

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0028_remove_track_play_days_remove_track_play_hours_and_more'),
    ]

    operations = [
        migrations.CreateModel(
            name='UploadConfig',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('max_batch_size_mb', models.PositiveIntegerField(default=100, help_text="Maximum combined size of all files in one upload action (all files dragged/selected together count as one batch, not per individual track). Enforced by the app itself on top of nginx's own hard ceiling (client_max_body_size, set in /etc/nginx/sites-available/isadoraair) -- raising this above that nginx value has no effect, since nginx rejects the request before Django ever sees it.", verbose_name='Max batch size (MB)')),
            ],
            options={
                'verbose_name': 'Upload Configuration',
                'verbose_name_plural': 'Upload Configuration',
            },
        ),
    ]
