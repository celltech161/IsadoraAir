# Generated by Django 5.2.15 on 2026-07-30 04:38

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0072_category_rbds_pty_override'),
    ]

    operations = [
        migrations.AlterField(
            model_name='category',
            name='rbds_ptyn',
            field=models.CharField(blank=True, default='', help_text="Optional 8-character Program Type Name sent alongside the PTY while a track from this category is playing (e.g. 'CLASSIC' or 'LOCAL'). Longer text is truncated to 8 characters. Blank clears any PTYN from the previous track rather than leaving a stale one on air. UECP-only: StereoTool's ASCII dialect has no PTYN command, so this field is silently ignored when Protocol is ASCII mode at /admin/rbds/rbdsconfig/1/ change/ (the value is kept regardless so switching back to UECP doesn't lose it). The PTY override above still applies in both protocols.", max_length=8, verbose_name='RBDS PTYN'),
        ),
    ]
