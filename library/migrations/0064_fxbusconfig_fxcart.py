# Generated by Django 5.2.15 on 2026-07-24 02:07

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0063_tuneinconfig'),
    ]

    operations = [
        migrations.CreateModel(
            name='FXBusConfig',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('volume_db', models.FloatField(default=0.0, help_text='FX bus output volume in dB. Applied to the sub-mixer output before it joins the main mixer. 0.0 = unity, negative for softer, positive for hotter. Range -24 to +6.')),
                ('polyphony_cap', models.PositiveSmallIntegerField(default=4, help_text='Maximum simultaneous fires. A 5th click while 4 fires are still in flight is dropped. Range 1-8; higher values just increase potential CPU on the engine.')),
            ],
            options={
                'verbose_name': 'FX Bus',
                'verbose_name_plural': 'FX Bus',
            },
        ),
        migrations.CreateModel(
            name='FXCart',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(help_text='Button label shown on the dashboard + remote-DJ. Keep it short (~15 chars renders cleanly).', max_length=40)),
                ('filepath', models.CharField(help_text='Absolute path to the audio file (e.g. /srv/isadoraair/carts/laugh.wav). Any format the engine can decode (mp3/flac/wav/m4a/ogg/aiff).', max_length=512)),
                ('gain_db', models.FloatField(default=0.0, help_text='Per-cart trim, dB. Applied on top of the global FX Bus volume. Range roughly -12 to +6 -- go bigger only if you know why.')),
                ('idle_color', models.CharField(default='rgba(55, 65, 81, 1.00)', help_text='Button color when idle (RGBA). Any CSS color works.', max_length=50)),
                ('playing_color', models.CharField(default='rgba(59, 130, 246, 1.00)', help_text='Fill color that sweeps left->right across the button as the cart plays.', max_length=50)),
                ('retrigger_mode', models.CharField(choices=[('restart', 'Restart (cancel and re-fire)'), ('ignore', 'Ignore (drop second click)'), ('stop', 'Stop (halt current fire)')], default='restart', max_length=10)),
                ('keyboard_shortcut', models.CharField(blank=True, default='', help_text='Optional single character (1-9 or A-Z) that fires this cart when the browser tab has focus. Blank = click-only. Unique across all carts.', max_length=1)),
                ('sort_order', models.PositiveIntegerField(default=0)),
                ('enabled', models.BooleanField(default=True)),
            ],
            options={
                'verbose_name': 'FX Cart',
                'verbose_name_plural': 'FX Carts',
                'ordering': ['sort_order', 'name'],
            },
        ),
    ]
