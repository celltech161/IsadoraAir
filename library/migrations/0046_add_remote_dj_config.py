# Generated by Django 5.2.15 on 2026-07-10 00:16

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0045_add_nav_menu_item'),
    ]

    operations = [
        migrations.CreateModel(
            name='RemoteDJConfig',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('enabled', models.BooleanField(default=False, help_text="Master switch. While off, the engine's pipeline is completely unchanged (no remote-DJ tee or pads exist at all) and the signaling server isn't started.")),
                ('stun_server', models.CharField(default='stun://stun.l.google.com:19302', help_text='Public STUN server for ICE. Sufficient unless both ends are behind symmetric NAT -- a TURN relay (coturn) is deliberately not set up ahead of a real need for one.', max_length=200)),
                ('ice_udp_min_port', models.PositiveIntegerField(default=40000, help_text='Pinned to a small explicit range (rather than the wide ephemeral default) so router UDP forwarding has a known, small target.')),
                ('ice_udp_max_port', models.PositiveIntegerField(default=40010)),
            ],
            options={
                'verbose_name': 'Remote DJ Configuration',
                'verbose_name_plural': 'Remote DJ Configuration',
            },
        ),
    ]
