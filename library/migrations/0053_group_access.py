# Generated by Django 5.2.15 on 2026-07-23 03:32

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('auth', '0012_alter_user_first_name_max_length'),
        ('library', '0052_track_uploaded_by'),
    ]

    operations = [
        migrations.CreateModel(
            name='GroupAccess',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('allowed_prefixes', models.TextField(blank=True, help_text='One path prefix per line. A request whose path startswith any listed prefix is allowed. Example lines: /library/  /api/tracks/')),
                ('allowed_exact', models.TextField(blank=True, help_text='One exact path per line. The request path must equal the listed value literally (no startswith). Example lines: /api/tracks/  /api/engine/queue/insert/')),
                ('allowed_regex', models.TextField(blank=True, help_text="One Python regex per line, matched via re.match against the request path. For parameterized URLs a plain prefix or exact match can't cover. Example: ^/api/playlists/\\d+/play-now/$")),
                ('landing_url', models.CharField(blank=True, help_text="Where members of this group are redirected on '/' or on a denied page (page-shaped requests only; APIs still 403). Raw URL path, e.g. '/library/'. Blank = redirect to /welcome/.", max_length=200)),
                ('priority', models.IntegerField(default=100, help_text='Lower number wins when a user in multiple groups needs a landing page. Ties broken by group name (alphabetical).')),
                ('group', models.OneToOneField(help_text='The Django auth Group this access set applies to.', on_delete=django.db.models.deletion.CASCADE, related_name='access', to='auth.group')),
            ],
            options={
                'verbose_name': 'Group Access',
                'verbose_name_plural': 'Group Access',
                'ordering': ['priority', 'group__name'],
            },
        ),
    ]
